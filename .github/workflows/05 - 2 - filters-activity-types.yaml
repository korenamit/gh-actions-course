name: 05 - 2 - Event Filters and Activity Types

on:
    pull_request:
        types: 
            - closed
        branches: 
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            #- run: echo "Event Name: ${{ github.event_name }}"
            - run: echo "Runing whenever a pull request is closed"