name: 05 - 1 - Event Filters and Activity Types

on:
    pull_request:
        types: 
            - opened
            - synchronize
        branches: 
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            #- run: echo "Event Name: ${{ github.event_name }}"
            - run: echo "Runing whenever a pull request is opened or synchronized AND the base branch is main"